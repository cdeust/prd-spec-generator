{
  "name": "prd-spec-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Scientifically-backed PRD generation system with deterministic validation",
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "clean": "pnpm -r clean",
    "verify": "pnpm install --frozen-lockfile && pnpm build && pnpm test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}